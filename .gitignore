node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment — never commit real credentials
.env

# Generated at install time — not for git
CREDENTIALS.txt
# Release build artifacts
release/
innosetup-*.exe

# Application logs (daily-rotating, 30-day retention — see server/logger.ts)
logs/
