You are a senior full-stack engineer working on an existing Pharmacy POS:

- Backend: Node.js + Express.js
- Frontend: React.js (SPA)
- There is already a working “New Sale / POS” page with invoice creation, hold bill, etc.

NEW REQUIREMENTS (New Sale page):

1) Invoice number behaviour:
   - Invoice number should be **auto-generated by the backend when saving** (auto on save).
   - The invoice number field is NOT manually editable.
   - Before saving, the UI can show a placeholder like “Invoice: Auto on save”.
   - After a successful save, the actual invoice number from the backend should be:
     - Displayed on the New Sale page (header/summary area).
     - Available for reprint / future reference.

2) Reprint invoice:
   - The previous “Reprint invoice” option disappeared; bring back a **Reprint Bill** feature.
   - On the New Sale page, add a **“Reprint Bill” button before the “Hold” button**.
   - When user clicks “Reprint Bill”:
     - They should be able to select an invoice (e.g. by entering invoice number or choosing from a recent list).
     - After selecting, the full bill (header + medicine line items) should appear in **view-only mode**.
     - From this view-only screen/dialog, user can **print the invoice**.
   - The reprint view must NOT allow editing of items, quantities, prices, or totals.

IMPORTANT:
- Do NOT break existing invoice creation and hold/resume logic.
- Do NOT change existing API field names; only extend behaviour.
- Existing invoices and data must remain valid.
- Keep behaviour backward compatible.

────────────────────
1) Backend – invoice number auto generation
────────────────────

1.1 Invoice number generation:
- In the POST `/api/invoices` (or equivalent) endpoint:
  - If an invoice_number is not provided (or if you decide invoice_number should always be generated on server):
    - Generate a new invoice_number on save.
      - e.g. a sequential number per pharmacy, or a pattern like `INV-YYYYMMDD-XXXX`.
  - Ensure invoice_number is:
    - Unique per pharmacy.
    - Stored in the invoices table.
  - Return invoice_number in the response body along with the invoice data.

- If there is existing logic for invoice_number:
  - Keep it, but enforce that the final number is always set by the backend (not by the frontend).
  - Ignore any invoice_number field coming from the client unless you explicitly support pre-assigned numbers.

1.2 Optional: pre vs post invoice number behaviour
- Add a configuration flag (even if hardcoded for now) such as:
  - `invoiceNumberMode = 'POST_SAVE'`
- For now, implement only POST_SAVE:
  - Invoice number is only assigned at the time of saving.
  - Client displays “Invoice: Auto on save” until the save is successful; then shows the generated number from the response.

────────────────────
2) Frontend – New Sale page invoice display
────────────────────

2.1 Invoice number display:
- On the New Sale component:
  - Add state for `invoiceNumber` (initially null or empty).
  - In the header/summary area, display:
    - If invoiceNumber is null: “Invoice: Auto on save”
    - If invoiceNumber is set: “Invoice: {invoiceNumber}”
- When the save/generate invoice API call succeeds:
  - Read `invoice_number` from the response.
  - Store it in `invoiceNumber` state.
  - Also keep any id or meta needed for reprint.

2.2 Make invoice number non-editable in UI:
- Remove any manual input for invoice number from the New Sale form.
- If there is currently an invoice number input:
  - Convert it to a read-only/disabled field bound to `invoiceNumber`.
  - Before save, show placeholder or “Auto on save”.

────────────────────
3) Backend – support reprint invoice
────────────────────

3.1 Get invoice for viewing/printing:
- Ensure there is an endpoint to fetch a **single invoice with its line items**, e.g.:
  - GET `/api/invoices/:id` OR
  - GET `/api/invoices/by-number/:invoiceNumber`
- Response should include:
  - Invoice header (number, date/time, customer, totals, payment mode, etc.)
  - Line items array (product name, batch, quantity, prices, gst, etc.)
- Do not change existing response fields; if needed, extend them.

3.2 Optional: recent invoices for selection:
- Add an endpoint for listing recent invoices for the current pharmacy, e.g.:
  - GET `/api/invoices/recent?limit=50`
  - Return id, invoice_number, invoice_date, total_amount, customer_name.
- This will help the UI show a “recent bills” list for quick reprint.

────────────────────
4) Frontend – “Reprint Bill” button & view-only reprint UI
────────────────────

4.1 Add “Reprint Bill” button:
- On the New Sale page action area (where buttons like Hold/Save are), add a new button **before** the “Hold” button:
  - Label: “Reprint Bill”
- This button should open a **Reprint Bill dialog/page**.

4.2 Reprint Bill flow:
- When “Reprint Bill” is clicked:
  - Show a modal dialog or side panel with:
    - Option A: A simple input to type an Invoice Number and a “Load” button.
    - Option B (better): Invoice Number input + a table of recent invoices (using `/api/invoices/recent`).
  - If user selects an invoice (by typing number and pressing Enter or clicking a row):
    - Call the invoice detail API (`/api/invoices/:id` or `/by-number/...`).
    - Load the invoice data into a **view-only layout**.

4.3 View-only invoice screen:
- Display:
  - Header: Pharmacy details, invoice number, date, customer, etc.
  - Line items table:
    - Product name, batch, qty, unit price, gst, line totals.
  - Summary: subtotal, CGST, SGST, grand total, payment mode.
- All fields are **read-only**:
  - No editable inputs.
  - No quantity/price changes.
  - No “Save/Update” actions for this view.
- Add a **“Print Invoice”** or **“Print”** button:
  - On click, trigger `window.print()` or open a print-optimized preview if already implemented.
- Provide a “Close” / “Back” button to go back to the New Sale page without modifying data.

4.4 Ensure edit vs view separation:
- The existing New Sale component should remain the place where edits happen.
- The Reprint view is strictly for viewing/printing existing invoices:
  - No changes to DB from that screen.

────────────────────
5) Safety & regression checks
────────────────────

- Make sure that:
  - Auto-generated invoice numbers appear correctly on both:
    - The New Sale success screen.
    - The Reprint view.
  - Reprint Bill button does NOT interfere with:
    - New Sale / holding bill / resuming bill.
  - If invoice fetch fails (wrong number, not found), show a clear error in the Reprint dialog.

- Existing flows:
  - Creating a new invoice still works.
  - Holding and resuming bills still works.
  - No existing API contract is broken.

Implement all of the above with minimal changes to existing logic, reusing existing invoice models and components, and keeping the rest of the POS behaviour unchanged.
