You are a senior full-stack engineer working on an existing Pharmacy Management web app (Node/Express backend, Angular + Angular Material frontend). DO NOT rewrite architecture or remove existing features. Only ADD/MODIFY what is needed to fix these issues while keeping current behaviour intact.

Please:

1) Medicine modification / delete individual medicine
- In the Medicines / Product Master screen on the web app, add proper “Edit” and “Delete” actions for each medicine row.
- Edit should open the existing form prefilled, allow update, and save via the existing update API (do NOT create a new API if one exists).
- Delete should delete that single medicine (or mark it inactive) using the existing delete API, and then refresh the list.
- Confirm that delete only affects that one medicine and does NOT break foreign key relations for billing/purchases.

2) Unique number in every new sale
- In the Billing / New Sale screen, ensure each new sale has a unique invoice/sale number generated by the backend (e.g. incremental or timestamp-based).
- Show this unique number clearly on the Billing screen as soon as the sale is created or about to be saved.
- Make sure this number is stored with the invoice in the database and appears in the recent sales list.

3) Sorting items with header arrows (web)
- On all main tables (e.g. Medicines list, Suppliers list, Sales/Invoices list, Purchase list), enable sorting by clicking the header arrows.
- Use Angular Material mat-sort (or the existing table sorting mechanism) so clicking a column header sorts ascending/descending.
- Ensure sorting works together with pagination and filtering, and does not break existing functionality.

4) Billing screen – customer name field auto stretch issue
- On the Billing / New Sale screen, fix the layout so the “Customer Name” field does not auto-stretch incorrectly.
- Make the Customer Name input have a reasonable fixed or flex-based width aligned with other fields, and keep the layout responsive.
- Do not change field names or remove controls; only adjust the HTML/CSS/Angular Material layout.

5) “Add/Search Medicine” instead of only search
- On the Billing screen, change the UX so the user can BOTH search and add medicine:
  - Update the label to “Add/Search Medicine”.
  - If possible, allow adding a new medicine from this screen (e.g. open a dialog with the existing medicine creation form) without breaking the normal search behaviour.
- Make sure the existing search flow still works exactly as before for existing medicines.

6) Discount percentage instead of amount
- On the Billing / New Sale screen, change discount handling from a fixed amount to a percentage-based discount.
- The discount field should accept a percentage value (e.g. 5, 10, 15) and the system should calculate the discount amount automatically and apply it to the total.
- If there is a master/config screen for discount rules, reuse it or add a simple configuration there so behaviour is consistent.
- Ensure totals (subtotal, GST, grand total) are still correct after applying percentage discount.

7) Resume bill – “failed to delete held up” popup
- When resuming a held bill, fix the logic so that:
  - The held/parked bill is properly loaded into the Billing screen.
  - After successful resume and save, the original “held” entry is correctly deleted/cleared from the hold/park table.
- Remove the incorrect error popup “failed to delete held up” if the delete actually succeeds.
- Make sure errors are only shown when there is a real backend failure.

General requirements:
- Search the existing codebase for the relevant components/services and extend them instead of creating duplicates.
- Keep all API endpoints, types, and URLs consistent.
- Do not break any existing flows (login, existing billing, existing lists).
- After changes, run the app and manually verify the above flows on the web UI.
